# Core backend dependencies (lean cloud image). Voice + image OCR extras live in
# requirements-optional.txt and are only needed when ENABLE_VOICE / ENABLE_OCR are on.
# torch is installed as the CPU-only build in the Dockerfile to keep the image small.
fastapi==0.135.3
uvicorn[standard]==0.44.0
pymongo==4.17.0
bcrypt==4.3.0
PyJWT==2.10.1
pydantic==2.12.5
email-validator==2.3.0
python-multipart==0.0.20
python-dotenv==1.2.2
requests==2.31.0
pypdf==4.2.0
scikit-learn==1.8.0
numpy==2.2.6
torch==2.9.1
sentence-transformers==5.4.1
mistralai==0.4.2
groq==1.2.0
